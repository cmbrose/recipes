#-------------------------------------------------
# Bundler / Ruby
#   Keep Gemfile + Gemfile.lock, but ignore local installs
.bundle/
vendor/bundle/

# Logs, temp files created by scripts or local Ruby runs
log/
tmp/

#-------------------------------------------------
# Jekyll build artifacts
_site/
.jekyll-cache/
.sass-cache/

#-------------------------------------------------
# Python (for import.py, export scripts)
__pycache__/
*.py[cod]

#-------------------------------------------------
# Node (if you add a JS build later)
node_modules/
npm-debug.log*

#-------------------------------------------------
# Backups / archives
*.bak
*.orig
*.old
*.zip
*.tar.gz